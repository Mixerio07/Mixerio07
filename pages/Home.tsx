export default function Home() {
  // Публичный сайт-визитка (самодостаточная статическая страница)
  return (
    <iframe
      src="/landing/index.html"
      title="КОНТЭК Инжиниринг"
      className="fixed inset-0 w-full h-full border-0"
    />
  );
}
